# Video → Text Transcriber

Run locally:
```bash
cp .env.example .env
docker compose up --build
```
Open:
- UI: http://localhost:3000
- API docs: http://localhost:4000/docs
- Storage console: http://localhost:9001 (minioadmin / minioadmin)

Deploy: use `render.yaml` and the One‑Click Deploy Kit.
